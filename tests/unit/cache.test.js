import assert from 'node:assert';
import { describe, it } from 'node:test';

describe('cache (placeholder)', () => {
  it('sanity: null is null', () => {
    assert.strictEqual(null, null);
  });
});
