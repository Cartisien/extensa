import assert from 'node:assert';
import { describe, it } from 'node:test';

describe('embedder (placeholder)', () => {
  it('sanity: basic arithmetic', () => {
    assert.strictEqual(1+1, 2);
  });
});
